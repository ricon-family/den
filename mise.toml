[settings]
experimental = true
quiet = true
task_output = "interleave"

[plugins]
shiv = "https://github.com/KnickKnackLabs/vfox-shiv"

[tools]
usage = "latest"
gum = "latest"
"shiv:shimmer" = "0.1.2"                 # Agent orchestration + identity
"shiv:sessions" = "0.4.1"                 # Session transcripts
"shiv:modules" = "latest"                  # Encrypted git submodules
"shiv:notes" = "latest"                    # Encrypted shared notes
"shiv:threads" = "latest"
"shiv:rudi" = "latest"                     # git-crypt wrapper — CI calls rudi install directly
"shiv:secrets" = "latest"                  # Provider-based secret management
"shiv:emails" = "latest"                   # Email CLI for agents
"shiv:codebase" = "latest"                 # Codebase linting + migrations
"shiv:farts" = "latest"                    # Frontmatter queries for agent:list

# git-crypt has no macOS ARM prebuilt binary — install via: rudi install

# Home — shared agent infrastructure
#
# Each agent has tasks in agents/<name>/.mise/tasks/
# Shared tasks go in .mise/tasks/
# Run `mise tasks` to see available tasks

[_.codebase]
lint = ["mise-settings", "gum-table"]

[_.codebase.scope]
gum-table = ".mise/tasks"
