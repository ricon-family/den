# Shared test helpers. Sourced by every .bats file via `load test_helper`.
#
# See fold's [[bats-tool-testing]] for the canonical "call the tool, not the
# script" pattern. For pure-function unit tests (no mise dispatch), source
# the lib directly inside the .bats file.
